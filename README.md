# interactive-dictionary
This project takes a word as input from the user and returns with the meaning of it. If the word doesn't exist, it returns the set of closely related words. It is a interactive dictionary that reads out the flow of execution.

Modules used:
os
gTTS (Google Text To Speech): To install use the command: "pip install gTTs"
difflib
playsound: To install use the command: "pip install playsound"
json

Download data.json file from the repository to read dictionary data.
The program is executed in Python 3 environment and the above commands are for Windows.

Blog: https://techprodezza.code.blog/
